//! Local server — owns filesystem, world folder, HTTP API, projects list.
//! Calls into mapkeeper-core for rules; HTTP framework choice is an open
//! implementation detail (not blocking repo layout, D-20).

fn main() {
    println!("mapkeeper-server: skeleton only — HTTP API not wired yet");
}
