{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables default launcher permissions (folder picker + open release URL)",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default", "opener:allow-default-urls"]
}
