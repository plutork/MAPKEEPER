# Author toolchain

## Create your world (recommended)

1. Open **MAPKEEPER** in Cursor (alpha: `.\setup.ps1` → `.\run.ps1`).
2. On Home, click **Create your first world** (Build wizard).
3. Work in your world folder (usually `Documents/MAPKEEPER Worlds`) — not in this product repo.

Details: [docs/CURSOR-ALPHA.md](../docs/CURSOR-ALPHA.md).

## Layout in this repo

| Path | Purpose |
|------|---------|
| [template/world/](template/world/) | Canonical world scaffold embedded in the editor (D-26) |
| [cursor/user.md](cursor/user.md) | Legacy manual install — dogfood only |

The public GitHub Template distribution path is **retired** (D-84). Do not point authors at `mapkeeper-world-template`.

## Later (V0+)

- Genre adapters as optional packs
- Specialist `/user` lenses (Geo, Time, Import, Canon)

Maintainer tooling lives outside this public repo.
