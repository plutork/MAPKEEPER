#!/bin/sh
# Block push when local checks fail (CI parity via scripts/check.ps1).
root="$(git rev-parse --show-toplevel)"
exec powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$root/scripts/check.ps1"
