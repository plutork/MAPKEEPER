# Schemas

JSON Schema contracts for mapkeeper data — cell profiles, `mapkeeper.toml`.

**Status:** empty. V0 minimum profile fields are roadmap 3.2 (open) — see the
`mapkeeper-cell-schema` skill and MAPKEEPER-OS `decisions.md` D-20 for the
`crates/core` model these schemas validate.
